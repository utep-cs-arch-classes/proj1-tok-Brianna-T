#include <stdio.h>

int main(){
  char user[20]://length of what the user's string can be
    //DONT USE GETS and PUTS
    printf("Type anything, $");
  for(char user;(user=getchar())&&i!>20;i++){
    putchar(i);
  }
  return 0; //closing main, exits
}
